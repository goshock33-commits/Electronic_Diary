# 时光日记 · 后端开发文档

> HarmonyOS NEXT 电子日记本 —— 后端(数据层 + 业务逻辑层 + 系统能力)开发说明文档

---

## 一、项目概述

后端负责**数据持久化、业务逻辑、系统能力集成**,是应用的数据与逻辑基座。采用 RelationalStore 关系型数据库为核心数据源,ViewModel 承载业务逻辑,通过 EventBus 与前端 UI 解耦通信。

- **后端职责**:数据库(建表/CRUD/迁移)、DAO 数据访问、ViewModel 业务逻辑、Service 服务、系统能力(服务卡片/AI/分布式/通知等)、Ability/扩展能力。
- **与前端边界**:前端通过 ViewModel 方法(Promise)、EventBus 事件、AppStorage 键消费后端;后端**不含 UI**。

---

## 二、技术栈

| 类别 | 技术 / Kit |
|------|-----------|
| 开发语言 | ArkTS(严格模式) |
| 关系型数据库 | `@ohos.data.relationalStore`(ArkData) |
| 偏好设置 | `@ohos.data.preferences` |
| 分布式同步 | `@ohos.data.distributedKVStore` + 分布式软总线 |
| 后台任务 | `@ohos.resourceschedule.backgroundTaskManager`(长时任务) |
| 提醒代理 | `@ohos.reminderAgentManager` |
| 通知 | `@ohos.notificationManager` |
| 服务卡片 | `FormExtensionAbility` / `formProvider`(FormKit) |
| AI 能力 | Core Speech Kit(语音) / Core Vision Kit(OCR) / Intents Kit(小艺意图) |
| 分享 | Share Kit + Harmony Share(碰一碰) |
| 加密 | `@ohos.security.cryptoFramework`(AES) |
| 传感器 | `@ohos.sensor`(加速度/摇一摇) |
| SDK | HarmonyOS 6.1.1(API 24) |

---

## 三、后端架构

### 3.1 分层与数据流

```
前端 UI(pages / components)
      │  ① 调 ViewModel 方法(Promise)   ② 订阅 EventBus 事件
      ▼
┌─────────────────────────────────────────────┐
│  ViewModel 业务逻辑层                         │  数据校验 + 业务规则 + 发 EventBus 事件
│  (features/*/viewmodel/*.ets)                 │
├─────────────────────────────────────────────┤
│  Service 服务层(部分模块)                     │  TaskExtractionService(AI) / PomodoroTimerService(计时)
├─────────────────────────────────────────────┤
│  DAO 数据访问层(shared/database/dao/*.ets)    │  增删改查,走 DatabaseExecutor
├─────────────────────────────────────────────┤
│  DatabaseExecutor(薄透传层)                   │  executeRead/executeWrite/executeInTransaction
│  DatabaseHelper(单例 + 建表 + 版本迁移)        │
├─────────────────────────────────────────────┤
│  RelationalStore 数据库(11 张表)              │
└─────────────────────────────────────────────┘
```

**关键设计**:DatabaseExecutor 为**薄透传层**(去 TaskPool),RelationalStore 原生异步 API 由系统线程池执行,天然不阻塞 UI;对外统一 `Promise<T>` 签名。

### 3.2 事件总线(EventBus)
业务变更通过 `EventBus.emit(EventBus.EventNames.XXX, payload)` 广播,前端订阅刷新,实现前后端解耦。

---

## 四、目录结构(后端相关)

```
Electronic_Diary/shared/src/main/ets/
├── database/
│   ├── DatabaseExecutor.ets     # 薄透传层(executeRead/Write/Transaction)
│   ├── DatabaseHelper.ets       # 数据库单例 + 建表 + 版本迁移
│   └── dao/                     # 数据访问对象
│       ├── DiaryDao.ets         # 日记
│       ├── TodoDao.ets          # 待办
│       ├── CheckInDao.ets       # 打卡
│       ├── PomodoroDao.ets      # 番茄钟
│       ├── QuoteDao.ets         # 名言
│       ├── ExtractedTaskDao.ets # 提取任务
│       ├── AppSettingsDao.ets   # 设置(KV)
│       ├── SectionConfigDao.ets # 分区配置
│       └── ...
├── data/                        # 数据模型(Diary/Todo/CheckIn...)
├── utils/
│   └── EventBus.ets             # 事件总线(string 事件名 + 扁平载荷)
└── state/                       # 全局状态(AppState 键定义)

Electronic_Diary/features/*/src/main/ets/
├── viewmodel/                   # 业务逻辑层
├── services/                    # 服务(TaskExtractionService / PomodoroTimerService)
└── model/                       # 模块数据模型

Electronic_Diary/entry/src/main/ets/
├── entryability/                # Ability(主入口/编辑/安全锁/任务确认)
├── extensionabilities/          # ShareReceiveAbility(分享接收)等
└── formextensions/              # 服务卡片扩展
```

---

## 五、数据库设计

### 5.1 数据表(11 张)

| 表 | 说明 | 关键字段 |
|----|------|----------|
| `diary` | 日记 | id, date(UNIQUE), title, mood, content, images, created_at, updated_at |
| `todo` | 待办 | id, content, date, type(today/tomorrow), completed, due_date |
| `check_in` | 打卡 | id, date(UNIQUE), streak_days |
| `pomodoro` | 番茄钟 | id, preset_type, actual_seconds, completed, ended_at |
| `extracted_task` | 提取任务 | id, title, task_type, task_date, task_time, location, is_converted |
| `quote` | 名言 | id, content, author, is_builtin, is_favorite |
| `app_settings` | 设置(KV) | key(UNIQUE), value |
| `section_config` | 分区配置 | id, section_key, sort_order, visible |
| `custom_emoji` | 自定义心情 | emoji_key(UNIQUE), label, type, value, sort_order |

> 注:`happy_thing`(开心的事)、`extra_thing`(多做的事)按产品决策将移除(只删代码,表保留闲置,不 DROP)。

### 5.2 规范(PROJECT_SPEC §8.2)
- 列类型:`ColumnType.INTEGER`(**无 FLOAT**)、TEXT。
- boolean 存 `number`(0/1)。
- 日期统一 `"YYYY-MM-DD"` 字符串。
- 索引:diary.date(UNIQUE)、diary.mood、check_in.date(UNIQUE) 等。

### 5.3 存储安全
- `DatabaseHelper` 初始化 StoreConfig 指定 `securityLevel: relationalStore.SecurityLevel.S2`(存储加密,上架审核要求)。
- 批量写用 `executeInTransaction`(WAL 模式,避免批量性能下降约 50 倍)。

---

## 六、DAO 数据访问层

所有 DAO 走 `DatabaseExecutor`,统一异步 `Promise<T>`。以 DiaryDao 为例:

```typescript
static async queryByDate(date: string): Promise<Diary | null> {
  return DatabaseExecutor.executeRead<Diary | null>('DiaryDao.queryByDate',
    async (store: relationalStore.RdbStore): Promise<Diary | null> => {
      const p = new relationalStore.RdbPredicates('diary');
      p.equalTo('date', date);
      const rs = await store.query(p);
      if (rs.goToFirstRow()) { const d = DiaryDao.rowToDiary(rs); rs.close(); return d; }
      rs.close();
      return null;
    });
}
```

- **读**:`executeRead` + `store.query/querySql` + ResultSet 遍历 + `rowToXxx` 映射。
- **写**:`executeWrite` + `store.insert/update/delete` + ValuesBucket。
- **事务**:`executeInTransaction` + `beginTransaction/commit/rollBack`。
- ⚠️ ResultSet 用完必须 `close()`。

---

## 七、ViewModel 业务逻辑层

- 位于 `features/*/viewmodel/`,承载业务规则、数据校验、组装。
- 对前端暴露 `Promise<T>` 方法(如 `CheckInViewModel.getTodayStatus() / checkIn()`)。
- 业务变更后发 EventBus 事件通知前端刷新。
- **字段命名蛇形**(snake_case),前端做映射 —— 改字段名需通知前端。

**Service 服务**(复杂逻辑抽出):
- `TaskExtractionService`:小艺意图调用封装 + 降级规则引擎。
- `PomodoroTimerService`:计时核心(保留 TaskPool 运行 + BackgroundTaskManager 长时任务)。

---

## 八、EventBus 事件总线

- 封装于 `shared/utils/EventBus.ets`,事件名走 `EventBus.EventNames.*` 常量。
- **发事件(§8.3 合规写法)**:
  ```typescript
  const payload: Record<string, Object> = {};
  payload['date'] = date;
  payload['isChecked'] = isChecked;
  EventBus.emit(EventBus.EventNames.CHECKIN_CHANGED, payload);
  ```
- ⚠️ **技术债**:当前存在「三通道割裂」(部分 VM 绕过 EventBus 裸发 string),需统一收编进 EventBus,否则前端跨页刷新易失效(详见 `workSpace/BACKEND_DEV_SPEC.md` §10.1)。

---

## 九、数据库版本迁移机制

**⚠️ 鸿蒙 API 24 的 `StoreConfig` 没有 `onUpgrade` 回调**(与 Android 不同),采用**手动步进迁移**:

```typescript
const DATABASE_VERSION: number = 2;
private async migrate(store: relationalStore.RdbStore): Promise<void> {
  let v: number = store.version;
  if (v === 0) { v = 1; store.version = 1; }        // 老库归一化基线 1
  if (v < 2) {
    await store.backup('backup.db');                 // 迁移前备份(每条都 await)
    try {
      // PRAGMA table_info 幂等守卫 → ALTER ADD COLUMN → CREATE INDEX
      store.version = 2;                             // DDL 全落地才写 version
    } catch (e) {
      await store.restore('backup.db');              // 失败回滚
      throw e as Error;
    }
  }
}
```

**原则**:基线冻结(新装建最新 schema)、幂等守卫(PRAGMA 判列存在)、全程 await、失败回滚、**禁止 DROP TABLE / RENAME TABLE**(重构用新增表+数据迁移)。

---

## 十、系统能力集成

| 能力 | Kit / API | 说明 |
|------|-----------|------|
| 每日提醒 | reminderAgentManager | 后台代理提醒(应用未运行也能提醒) |
| 番茄钟后台计时 | BackgroundTaskManager | 长时任务(audioPlayback),PersistentStorage 恢复 |
| 服务卡片 | FormExtensionAbility + formProvider | 桌面 4 卡,updateForm 推真实数据 |
| 任务提取 AI | Intents Kit(小艺意图) | intent_schema:ExtractTask + 5 槽位;降级本地规则 |
| 语音 | Core Speech Kit | 语音转文字 |
| 手势日记 | Core Vision Kit(OCR)+ Sensor + Camera | 摇一摇→拍照→识别生成建议 |
| 碰一碰 | Harmony Share + distributedKVStore | 跨设备传图/备份(LWW 冲突) |
| 数据加密 | cryptoFramework(AES-GCM) | 备份导出加密 |

> 各能力实现进度与详细施工指引见 `workSpace/BACKEND_DEV_SPEC.md`。

---

## 十一、开发规范(§8.3 编译坑,重点)

后端曾一次性触发 92 个编译错误,归为 7 类,**每次提交前对照自查**:

1. **`Record<string,Object>` 对象字面量**(如 emit 载荷)→ 必用「空字面量 + 括号逐键赋值」,禁平铺/as 断言。
2. **AppStorage 禁 import**(全局对象,`@kit.ArkUI` 只有 AppStorageV2)。
3. **跨模块引用用 `@ohos/shared`**(不是 `'shared'`)。
4. **禁解构声明** → 逐属性赋值。
5. **spread 仅限数组** → concat。
6. **禁 any/unknown** → 显式类型。
7. **API 名对照本地 `.d.ts`**(先查官方文档 → grep 本地 `@ohos.*.d.ts` API 24 → 再写)。

其他:§8.2 数据库规范;只改 src,**绝不碰 `oh_modules/`**;每块真机编译验证。

---

## 十二、待完善功能(施工清单)

详见 **`workSpace/BACKEND_DEV_SPEC.md`**(含代码骨架 + 官方文档入口),按优先级:

- **P0**:数据层(diary title/mood、DiaryDao queryAll/delete、番茄回写、昵称持久化、custom_emoji、**版本迁移机制**、搜索历史/书签位置)+ 删除开心的事/多做的事。
- **P1**:EventBus 统一、提醒代理后台化、数据导出加密、安全锁错误锁定、服务卡片真实刷新、任务提取 AI(降级版)。
- **P2**:语音识别、手势日记 AI(OCR)、实况窗、分布式碰一碰。

---

## 十三、构建与运行

1. **环境**:DevEco Studio + HarmonyOS SDK 6.1.1(API 24)。
2. 用 DevEco 打开 `Electronic_Diary/`,`ohpm install` 拉依赖。
3. 首次运行:`EntryAbility → StartupTasks → 数据库初始化`(建库建表)。
4. 连真机 / 模拟器 → Run。
5. **每块改动务必真机编译验证**;数据库迁移涉及老数据,务必 backup 兜底。

---

> 后端遵循「数据驱动、分层解耦、只改后端不动前端」原则。前后端通过 ViewModel 方法 + EventBus + AppStorage 协作,接口契约见 `workSpace/API_CONTRACT.md`。
